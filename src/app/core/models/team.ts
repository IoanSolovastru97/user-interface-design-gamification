import {Student} from './student';

export class Team {
  name: string;
  image: string;
  description: string;
  persons: Student[];
  numberOfPersons: number;
  innovation: number;
  teamwork: number;
  organization: number;
  communication: number;
}
